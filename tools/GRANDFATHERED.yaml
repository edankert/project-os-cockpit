# Grandfather ledger — debt each gate carried when it was promoted to error.
#
# Read by tools/scripts/validate-docs.py. An item listed here reports as a
# WARNING for that gate; every other item is an ERROR. There is no date-based
# exemption: editing a listed note does not re-arm the gate on it, and editing
# an unlisted note does not exempt it.
#
# This file only shrinks. When an item's debt is paid, delete its line — a
# stale entry is inert but misleading. Regenerate with:
#     python3 tools/scripts/grandfather.py --write --refresh
#
# ADR-0011 clause 3 prefers clearing debt to listing it. Entries here are an
# admission that the debt was not cleared, and each one is a small backlog item.
version: 1
cutover: "2026-07-25"
count: 77
gates:
  VERIFY: {}
  REQ-BOXES:
    REQ-0021: "REQ-0021 is 'implemented' (terminal) but 5 acceptance criterion/criteria remain unticked (docs/requirements/REQ-0021-Live-Progress-Visibility.md); ..."
  FEATURE-REQ:
    FEAT-0038: "FEAT-0038 is done but a requirement it owns has unresolved acceptance criteria: REQ-0021 (5 unticked); tick with evidence, reconcile, or descope th..."
  PHASE-CHILDREN:
    PHASE-007: "PHASE-007 is 'done' but ISS-0024 (open) still names it; the issue is live cockpit work (status-surface + CSS guard gaps), not stale metadata — resolve..."
  PHASE-BOXES:
    PHASE-001: "PHASE-001 is done but records no exit criteria (docs/phases/PHASE-001-*.md); the phase notes predate the PHASE-BOXES gate and were written without an Exit Criteria section at all..."
    PHASE-002: "PHASE-002 is done but records no exit criteria (docs/phases/PHASE-002-*.md); the phase notes predate the PHASE-BOXES gate and were written without an Exit Criteria section at all..."
    PHASE-004: "PHASE-004 is done but records no exit criteria (docs/phases/PHASE-004-*.md); the phase notes predate the PHASE-BOXES gate and were written without an Exit Criteria section at all..."
    PHASE-005: "PHASE-005 is done but records no exit criteria (docs/phases/PHASE-005-*.md); the phase notes predate the PHASE-BOXES gate and were written without an Exit Criteria section at all..."
    PHASE-006: "PHASE-006 is done but records no exit criteria (docs/phases/PHASE-006-*.md); the phase notes predate the PHASE-BOXES gate and were written without an Exit Criteria section at all..."
    PHASE-007: "PHASE-007 is done but 6 exit criterion/criteria remain unticked (docs/phases/PHASE-007-Agent-Instrumentation.md); tick each with an evidence pointe..."
  PARENT-BACKLINK:
    ISS-0002: "ISS-0002 declares parent: FEAT-0019, but FEAT-0019 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0002-Zshrc-Al..."
    ISS-0003: "ISS-0003 declares parent: FEAT-0019, but FEAT-0019 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0003-Hook-Pat..."
    ISS-0004: "ISS-0004 declares parent: FEAT-0017, but FEAT-0017 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0004-Requirem..."
    ISS-0005: "ISS-0005 declares parent: FEAT-0020, but FEAT-0020 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0005-Nav-Row-..."
    ISS-0006: "ISS-0006 declares parent: FEAT-0020, but FEAT-0020 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0006-Rail-Mar..."
    ISS-0007: "ISS-0007 declares parent: FEAT-0027, but FEAT-0027 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0007-Stale-Ur..."
    ISS-0008: "ISS-0008 declares parent: FEAT-0007, but FEAT-0007 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0008-Terminal..."
    ISS-0009: "ISS-0009 declares parent: FEAT-0020, but FEAT-0020 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0009-Popovers..."
    ISS-0010: "ISS-0010 declares parent: FEAT-0020, but FEAT-0020 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0010-Agent-St..."
    ISS-0011: "ISS-0011 declares parent: FEAT-0034, but FEAT-0034 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0011-Follow-M..."
    ISS-0031: "ISS-0031 declares parent: FEAT-0041, but FEAT-0041 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0031-Plans-An..."
    ISS-0062: "ISS-0062 declares parent: FEAT-0046, but FEAT-0046 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0062-Most-Pla..."
    ISS-0063: "ISS-0063 declares parent: FEAT-0047, but FEAT-0047 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0063-Dead-Sta..."
    ISS-0064: "ISS-0064 declares parent: FEAT-0049, but FEAT-0049 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0064-Two-Revi..."
    ISS-0065: "ISS-0065 declares parent: FEAT-0050, but FEAT-0050 does not name it in `fixes:` / `issues:`; add it, or drop the parent (docs/issues/ISS-0065-Record-C..."
    TASK-0066: "TASK-0066 declares parent: FEAT-0008, but FEAT-0008 does not name it in `tasks:`; add it, or drop the parent (docs/features/cockpit-api-hardening/plan..."
    TASK-0067: "TASK-0067 declares parent: FEAT-0008, but FEAT-0008 does not name it in `tasks:`; add it, or drop the parent (docs/features/cockpit-api-hardening/plan..."
    TASK-0068: "TASK-0068 declares parent: FEAT-0008, but FEAT-0008 does not name it in `tasks:`; add it, or drop the parent (docs/features/cockpit-api-hardening/plan..."
    TASK-0069: "TASK-0069 declares parent: FEAT-0008, but FEAT-0008 does not name it in `tasks:`; add it, or drop the parent (docs/features/cockpit-api-hardening/plan..."
    TASK-0070: "TASK-0070 declares parent: FEAT-0011, but FEAT-0011 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-center-pane/plan/ta..."
    TASK-0071: "TASK-0071 declares parent: FEAT-0011, but FEAT-0011 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-center-pane/plan/ta..."
    TASK-0072: "TASK-0072 declares parent: FEAT-0011, but FEAT-0011 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-center-pane/plan/ta..."
    TASK-0073: "TASK-0073 declares parent: FEAT-0011, but FEAT-0011 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-center-pane/plan/ta..."
    TASK-0074: "TASK-0074 declares parent: FEAT-0011, but FEAT-0011 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-center-pane/plan/ta..."
    TASK-0075: "TASK-0075 declares parent: FEAT-0011, but FEAT-0011 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-center-pane/plan/ta..."
    TASK-0076: "TASK-0076 declares parent: FEAT-0013, but FEAT-0013 does not name it in `tasks:`; add it, or drop the parent (docs/features/agent-state-signal/plan/ta..."
    TASK-0077: "TASK-0077 declares parent: FEAT-0013, but FEAT-0013 does not name it in `tasks:`; add it, or drop the parent (docs/features/agent-state-signal/plan/ta..."
    TASK-0078: "TASK-0078 declares parent: FEAT-0013, but FEAT-0013 does not name it in `tasks:`; add it, or drop the parent (docs/features/agent-state-signal/plan/ta..."
    TASK-0079: "TASK-0079 declares parent: FEAT-0013, but FEAT-0013 does not name it in `tasks:`; add it, or drop the parent (docs/features/agent-state-signal/plan/ta..."
    TASK-0080: "TASK-0080 declares parent: FEAT-0010, but FEAT-0010 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-nav-right-pane/plan..."
    TASK-0081: "TASK-0081 declares parent: FEAT-0010, but FEAT-0010 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-nav-right-pane/plan..."
    TASK-0082: "TASK-0082 declares parent: FEAT-0010, but FEAT-0010 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-nav-right-pane/plan..."
    TASK-0083: "TASK-0083 declares parent: FEAT-0010, but FEAT-0010 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-nav-right-pane/plan..."
    TASK-0084: "TASK-0084 declares parent: FEAT-0010, but FEAT-0010 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-nav-right-pane/plan..."
    TASK-0085: "TASK-0085 declares parent: FEAT-0010, but FEAT-0010 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-nav-right-pane/plan..."
    TASK-0086: "TASK-0086 declares parent: FEAT-0010, but FEAT-0010 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-nav-right-pane/plan..."
    TASK-0087: "TASK-0087 declares parent: FEAT-0012, but FEAT-0012 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-ux/plan/tasks/TASK-..."
    TASK-0088: "TASK-0088 declares parent: FEAT-0012, but FEAT-0012 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-ux/plan/tasks/TASK-..."
    TASK-0089: "TASK-0089 declares parent: FEAT-0012, but FEAT-0012 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-ux/plan/tasks/TASK-..."
    TASK-0090: "TASK-0090 declares parent: FEAT-0012, but FEAT-0012 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-ux/plan/tasks/TASK-..."
    TASK-0091: "TASK-0091 declares parent: FEAT-0012, but FEAT-0012 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-ux/plan/tasks/TASK-..."
    TASK-0092: "TASK-0092 declares parent: FEAT-0012, but FEAT-0012 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-ux/plan/tasks/TASK-..."
    TASK-0093: "TASK-0093 declares parent: FEAT-0009, but FEAT-0009 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-shell-layout/plan/t..."
    TASK-0094: "TASK-0094 declares parent: FEAT-0009, but FEAT-0009 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-shell-layout/plan/t..."
    TASK-0095: "TASK-0095 declares parent: FEAT-0009, but FEAT-0009 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-shell-layout/plan/t..."
    TASK-0096: "TASK-0096 declares parent: FEAT-0009, but FEAT-0009 does not name it in `tasks:`; add it, or drop the parent (docs/features/native-shell-layout/plan/t..."
    TASK-0146: "TASK-0146 declares parent: FEAT-0027, but FEAT-0027 does not name it in `tasks:`; add it, or drop the parent (docs/features/external-signal/plan/tasks..."
    TASK-0169: "TASK-0169 declares parent: FEAT-0035, but FEAT-0035 does not name it in `tasks:`; add it, or drop the parent (docs/features/account-budget/plan/tasks/..."
    TASK-0170: "TASK-0170 declares parent: FEAT-0035, but FEAT-0035 does not name it in `tasks:`; add it, or drop the parent (docs/features/account-budget/plan/tasks/..."
    TASK-0171: "TASK-0171 declares parent: FEAT-0035, but FEAT-0035 does not name it in `tasks:`; add it, or drop the parent (docs/features/account-budget/plan/tasks/..."
    TASK-0172: "TASK-0172 declares parent: FEAT-0035, but FEAT-0035 does not name it in `tasks:`; add it, or drop the parent (docs/features/account-budget/plan/tasks/..."
    TASK-0174: "TASK-0174 declares parent: FEAT-0006, but FEAT-0006 does not name it in `tasks:`; add it, or drop the parent (docs/features/cockpit/plan/tasks/TASK-01..."
    TASK-0175: "TASK-0175 declares parent: FEAT-0030, but FEAT-0030 does not name it in `tasks:`; add it, or drop the parent (docs/features/agent-inbox/plan/tasks/TAS..."
    TASK-0176: "TASK-0176 declares parent: FEAT-0023, but FEAT-0023 does not name it in `tasks:`; add it, or drop the parent (docs/features/overview-scopes/plan/tasks..."
    TASK-0177: "TASK-0177 declares parent: FEAT-0023, but FEAT-0023 does not name it in `tasks:`; add it, or drop the parent (docs/features/overview-scopes/plan/tasks..."
    TASK-0178: "TASK-0178 declares parent: FEAT-0022, but FEAT-0022 does not name it in `tasks:`; add it, or drop the parent (docs/features/session-insight/plan/tasks..."
    TASK-0179: "TASK-0179 declares parent: FEAT-0033, but FEAT-0033 does not name it in `tasks:`; add it, or drop the parent (docs/features/signal-hygiene/plan/tasks/..."
    TASK-0180: "TASK-0180 declares parent: FEAT-0032, but FEAT-0032 does not name it in `tasks:`; add it, or drop the parent (docs/features/agents-screen/plan/tasks/T..."
    TASK-0181: "TASK-0181 declares parent: FEAT-0023, but FEAT-0023 does not name it in `tasks:`; add it, or drop the parent (docs/features/overview-scopes/plan/tasks..."
    TASK-0184: "TASK-0184 declares parent: FEAT-0030, but FEAT-0030 does not name it in `tasks:`; add it, or drop the parent (docs/features/agent-inbox/plan/tasks/TAS..."
    TASK-0185: "TASK-0185 declares parent: FEAT-0003, but FEAT-0003 does not name it in `tasks:`; add it, or drop the parent (docs/features/embedded-terminal/plan/tas..."
    TASK-0186: "TASK-0186 declares parent: FEAT-0003, but FEAT-0003 does not name it in `tasks:`; add it, or drop the parent (docs/features/embedded-terminal/plan/tas..."
    TASK-0198: "TASK-0198 declares parent: FEAT-0006, but FEAT-0006 does not name it in `tasks:`; add it, or drop the parent (docs/features/cockpit/plan/tasks/TASK-01..."
    TASK-0213: "TASK-0213 declares parent: FEAT-0025, but FEAT-0025 does not name it in `tasks:`; add it, or drop the parent (docs/features/agent-registry/plan/tasks/..."
    TASK-0226: "TASK-0226 declares parent: FEAT-0042, but FEAT-0042 does not name it in `tasks:`; add it, or drop the parent (docs/features/design-bench/plan/tasks/TA..."
    TASK-0234: "TASK-0234 declares parent: FEAT-0045, but FEAT-0045 does not name it in `tasks:`; add it, or drop the parent (docs/features/inbox/plan/tasks/TASK-0234..."
    TASK-0246: "TASK-0246 declares parent: FEAT-0049, but FEAT-0049 does not name it in `tasks:`; add it, or drop the parent (docs/features/review-desk-record/plan/ta..."
    TASK-0247: "TASK-0247 declares parent: FEAT-0049, but FEAT-0049 does not name it in `tasks:`; add it, or drop the parent (docs/features/review-desk-record/plan/ta..."
  TEST-FIELDS: {}
  WAIVER: {}
