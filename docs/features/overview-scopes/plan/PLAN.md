# Plan — FEAT-0023 Overview scopes

1. **TASK-0128 (Python).** `stats_payload(index, scope=…)` + `exit_criteria` + `scope` block; `Index.generation` counter; handler-side cache; tests (TST-0012).
2. **TASK-0129 (renderer).** Scope pane, scoped centre dashboard, right-pane Now + scope context; remove centre live banner.
3. **TASK-0130 (renderer).** Virtual `~overview` rels in history; scroll-preserving refresh; shared agent snapshot fetch; live-duration tick.

Ship 0128 with tests first; 0129/0130 land together (same surfaces).
