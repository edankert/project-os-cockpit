---
type: "[[task]]"
id: TASK-0197
title: "Upstream the routing to project-os and adopt it here — template-owned hook + generated subagents replace the local prototype"
status: done
phase: "[[PHASE-007-Agent-Instrumentation]]"
owner: unassigned
created: 2026-07-24
updated: 2026-07-24
source: []
parent: "[[FEAT-0039-Model-Routing-Subagents]]"
effort: "M"
due: ""
depends: ["[[TASK-0195-Phase-Pinned-Subagents]]", "[[TASK-0196-Routing-Hint-Hook]]"]
blocks: []
related: []
tests: []
---

# Upstream the routing and adopt it downstream

## Definition of Done

- [x] Upstream `~/Dev/repos/project-os/` owns the routing: `tools/adapters/claude-code/hooks/model-routing-hint.sh` (HC-008), a `UserPromptSubmit` entry in `hooks.json`, a `planner` subagent generated by `tools/scripts/generate-adapters.py`, and named `PLANNER_MODEL`/`REVIEWER_MODEL` pins.
- [x] The upstream reviewer pin no longer collides with the recommended session model (was `claude-opus-4-8` with Opus recommended for implementation — same model authored and reviewed).
- [x] Upstream docs updated: HC-008 contract in `tools/instructions/HOOKS.md`, "Model routing" section + hook table row in the Claude Code `ADAPTER.md`, and a `CHG-*` note.
- [x] This repo consumes the upstream artifacts: the hook lives at `tools/adapters/claude-code/hooks/model-routing-hint.sh`, `.claude/agents/*.md` are byte-identical to upstream's generated output, the local `.claude/hooks/` prototype is deleted, and `.claude/settings.json` points at the adapter path.
- [x] `bash tools/scripts/validate-docs.sh` passes in both repos.

## Steps

- [x] Add the hook, `hooks.json` entry, planner subagent, and model-pin constants upstream; regenerate adapters there.
- [x] Update upstream HOOKS.md + ADAPTER.md + CHG note.
- [x] Copy the upstream hook and both generated agents into this repo; delete the local prototype; re-point `.claude/settings.json`.
- [x] Update this repo's `CLAUDE.md` to name the upstream as the owner of these files.

## Notes

The copies here are byte-identical to upstream's generated output, so a future template sync plus generator run is a no-op for them — that is the drift guard, since this repo does not yet carry `generate-adapters.py`.

A full `tools/scripts/sync-project-os.sh ../project-os` was deliberately NOT run as part of this task: the upstream working tree currently carries unrelated uncommitted changes (instructions, skills, `tools/cockpit/`), so a wholesale sync would pull in churn far beyond model routing. Adopting the routing files narrowly keeps the blast radius to what was asked for; the full sync remains a separate, deliberate operation.
