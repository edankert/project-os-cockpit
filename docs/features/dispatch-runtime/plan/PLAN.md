# Plan — FEAT-0025 Dispatch runtime

1. **TASK-0135 (Python first — the ledger is the contract).** Dispatch record + session stamping + `dispatch_history` + queue-request store; tests (TST-0014).
2. **TASK-0134 (main).** `dispatch-queue.ts` + PTY/sidecar exports + poller hook + preload + renderer popover.
3. **TASK-0136 (CLI).** `cockpit dispatch` + desktop request pickup.
